# Copy to .env and fill in. Never commit .env.
LABEL_EXTRACTOR=vlm            # vlm | ocr | stub
ANTHROPIC_API_KEY=
VLM_MODEL=claude-sonnet-5
MAX_IMAGE_EDGE_PX=1600         # downscale before the API call -- biggest latency lever
MAX_BATCH_CONCURRENCY=10
