# Pinned to the versions the tests and every report in eval/out ran on, so a
# build next month installs the same stack that was measured.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
python-multipart==0.0.32
jinja2==3.1.6
pillow==12.3.0
numpy==2.4.6
rapidfuzz==3.14.6
pytesseract==0.3.13
httpx==0.28.1
